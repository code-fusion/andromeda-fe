//Aca hay que importar cada reducer y usar combine reducer para exportarlos todos juntos


//Combine reducers tiene que tener 1 campo por cada campo del estado del store
const reducers = (state = null) => state;

export default reducers;